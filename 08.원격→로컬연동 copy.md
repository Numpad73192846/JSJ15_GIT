# 원격 저장소 생성 후 로컬 저장소와 연결하기

1. Github에서 원격 저장소를 생성
2. git config --global user.name "사용자 이름"
   git config --global user.email "사용자 이메일"
3. git clone <URL>

## VS CODE에서 연동

(깃허브 인증)

1. 명령 팔레드 (Ctrl + Shift + P)
2. Git: Clone
   - URL 입력
   - Github에서 복제 → 인증된 내 Github의 저장소 선택
